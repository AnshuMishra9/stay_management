/* ============================================================
   Global application script (loaded on internal pages).
   Currently a placeholder for shared UI behaviour. The login
   page uses its own AngularJS module in auth.js.
   ============================================================ */
(function () {
    'use strict';
    // Shared behaviour for authenticated pages can be added here.
})();
